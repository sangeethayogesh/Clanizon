import React from 'react'
import PropTypes from 'prop-types'
import { Layout } from 'antd'
const { Content } = Layout
const CallStatusForm = props => {
  return (
    <div>
      <Layout>
        <Content></Content>
      </Layout>
    </div>
  )
}

CallStatusForm.propTypes = {}

export { CallStatusForm }
